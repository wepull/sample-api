# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test for Successful API Requests"
    flow:
      - log: "Starting flow to validate correct API behavior with valid input payloads"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
      - think: 1
