# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f
# 
# 

# ********RoostGPT********
config:
  target: "http://example.com" # Replace with your actual API host
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "id"
      - "name"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://example.com" # Replace with your actual API host
      headers:
        Authorization: "AUTH_TOKEN" # Replace with your actual auth token

scenarios:
  - name: "Test non-successful API responses"
    flow:
      - log: "Starting scenario for non-successful API requests"
      - post:
          url: "/grants/wrong-endpoint"
          headers:
            Authorization: "AUTH_TOKEN" # Replace with your actual auth token
          expect:
            - statusCode: 404
      - think: 1
      - get:
          url: "/grants/{{ id }}"
          headers:
            Authorization: "AUTH_TOKEN" # Replace with your actual auth token
          expect:
            - statusCode: 401
      - think: 1
      - post:
          url: "/grants"
          json:
            id: "{{ id }}"
            name: "invalid grant name"
          headers:
            Authorization: "AUTH_TOKEN" # Replace with your actual auth token
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                - "error.message"
                - "Invalid grant name"
      - think: 1
      - log: "Ending scenario for non-successful API requests"
