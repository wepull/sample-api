# Test generated by RoostGPT for test G683-customer-order_management using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products:search_get for http method type GET in artillery framework

# RoostTestHash=824ef8445c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products:search_get_success.csv"
    fields:
      - "brand_name"
      - "product_name"
      - "upc"
      - "model_number"
      - "confidence_model"
      - "product_description"
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: Search Products API Test Scenario
    flow:
      - comment: POST request to search products
        post:
          url: "/products:search?brand_name={{ brand_name }}&product_name={{ product_name }}&upc={{ upc }}&model_number={{ model_number }}&confidence_model={{ confidence_model }}&product_description={{ product_description }}"
          expect:
            - statusCode: 200 
            - hasProperty: 'source_integrity'
            - hasProperty: 'source_integrity.score'
            - hasProperty: 'source_integrity.source'
            - hasProperty: 'source_integrity.reason'
        think: 5
      - comment: Another request for retrying with different parameters could go here (followed by think time). 
