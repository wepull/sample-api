# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-successful API requests test"
    flow:
      - log: "POST request with invalid payload"
      - post:
          url: "/me"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "invalid"
            image: "invalid"
            settings: "invalid"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
      - think: 1

      - log: "POST request without auth token"
      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
      - think: 1

      - log: "POST request to non-existing URL"
      - post:
          url: "/non-existing"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
      - think: 1
