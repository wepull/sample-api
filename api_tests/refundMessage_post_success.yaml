# Test generated by RoostGPT for test api-gpt4-turbo using AI Type Open AI and AI Model gpt-4


# Test generated for /icd/s2s/exchange/refundMessage_post for http method type POST in artillery framework

# RoostTestHash=35c90a3284


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "refundMessage_post_success.csv"
    fields:
      - "headerVersion"
      - "timestamp"
      - "sourceId"
      - "applicationId"
      - "transactionId"
      - "messageContentType"
      - "messageContentEncoding"
      - "payloadHashValue"
      - "hashingAlgorithm"
      - "augmentationKey"
      - "augmentationValue"
      - "remark"
      - "documentNumber"
      - "issuanceDate"
      - "refundIndicator"
      - "refundUID"
      - "relatedRefundUID"
      - "oadCode"
      - "cityPointOfSale"
      - "areaPointOfSale"
      - "countryPointOfSale"
      - "issuerSystem"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Refund Message POST request"
    flow:
      - post:
          url: "/icd/s2s/exchange/refundMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            messageHeader:
              headerVersion: "{{ headerVersion }}"
              timestamp: "{{ timestamp }}"
              sourceContext:
                sourceId: "{{ sourceId }}"
                applicationId: "{{ applicationId }}"
                transactionId: "{{ transactionId }}"
              messageContext:
                messageContentType: "{{ messageContentType }}"
                messageContentEncoding: "{{ messageContentEncoding }}"
                payloadHashValue: "{{ payloadHashValue }}"
                hashingAlgorithm: "{{ hashingAlgorithm }}"
            augmentationPoint:
              - augmentationKey: "{{ augmentationKey }}"
                augmentationValue: "{{ augmentationValue }}"
            payload:
              refundTransactions:
                remark: "{{ remark }}"
                documentNumber: "{{ documentNumber }}"
                issuanceDate: "{{ issuanceDate }}"
                refundIndicator: "{{ refundIndicator }}"
                refundUID: "{{ refundUID }}"
                relatedRefundUID: "{{ relatedRefundUID }}"
                oadCode: "{{ oadCode }}"
              refundingAgentDetails:
                cityPointOfSale: "{{ cityPointOfSale }}"
                areaPointOfSale: "{{ areaPointOfSale }}"
                countryPointOfSale: "{{ countryPointOfSale }}"
                issuerSystem: "{{ issuerSystem }}"
          expect:
            - statusCode: 200
            - hasProperty: "messageHeader"
            - hasProperty: "payload"
        think: 1
