# ********RoostGPT********
# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Test generated for /verifyOTP_post for http method type POST in artillery framework
# RoostTestHash=cec9d6112c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "verifyOTP_post_others.csv"
    fields: ["customerId", "code"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10

scenarios:
  - name: "Non-Success API responses"
    flow:
      # Sending a POST request with an invalid payload
      - post:
          url: "/verifyOTP"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            code: "{{ code }}"
      - expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      # Sending a POST request without authentication
      - post:
          url: "/verifyOTP"
          headers:
            Content-Type: "application/json"
          json:
            customerId: "{{ customerId }}"
            code: "{{ code }}"
      - expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      # Sending a POST request to an incorrect URL
      - post:
          url: "/verifyOTPs"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            code: "{{ code }}"
      - expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
