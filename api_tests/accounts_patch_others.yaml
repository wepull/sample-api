# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_patch_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
scenarios:
  - name: "Test for non-successful API requests"
    flow:
      - log: "Sending invalid POST request to /accounts"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalidemail"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                message: "Invalid email format"
      - think: 1

      - log: "Sending POST request to /accounts without authorization token"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                message: "Unauthorized"
      - think: 1

      - log: "Sending POST request to an incorrect URL"
      - post:
          url: "/incorrectURL"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals: 
                message: "URL not found"
      - think: 1
