# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=c02d3561ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_others.csv"
    skipHeader: true
    fields:
      - id
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending a GET request with valid id"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a GET request with invalid id"
      - get:
          url: "/credentials/invalid-id"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending a GET request with missing authentication"
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending a GET request to an invalid URL"
      - get:
          url: "/invalid-url/{{ id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
