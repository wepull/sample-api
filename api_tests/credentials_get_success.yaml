# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "credential"
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test API responses"
    flow:
      - log: "Starting test for /credentials endpoint"
      - get:
          url: "/credentials?size=20&offset=0"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "total"
            - hasProperty: "credentials"
      - think: 1
      - log: "Starting test for /credentials/{id} endpoint"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
      - think: 1
