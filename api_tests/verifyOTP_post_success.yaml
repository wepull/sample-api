# ********RoostGPT********
# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Test generated for /verifyOTP_post for http method type POST in artillery framework
# RoostTestHash=cec9d6112c



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "verifyOTP_post_success.csv"
    fields:
      - "customerId"
      - "code"
    skipHeader: true

scenarios:
  - name: "Verify OTP API - Success Scenario"
    flow:
      - post:
          url: "/verifyOTP"
          json:
            customerId: "{{ customerId }}"
            code: "{{ code }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - expect:
          - statusCode: 200
          - hasProperty: "customerId"
          - hasProperty: "code"
