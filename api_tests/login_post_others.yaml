# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Login endpoint tests for non-success responses"
    flow:
      - log: "Sending invalid payload to the /login endpoint"
      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: "not_an_email"
            otp: "123456"
            client: "client"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending request to the /login endpoint without authorization token"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@test.com"
            otp: "123456"
            client: "client"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending request to the /login endpoint with incorrect OTP"
      - post:
          url: "/login"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: "test@test.com"
            otp: "wrong_otp"
            client: "client"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1

      - log: "Sending request to an incorrect URL"
      - post:
          url: "/incorrect_url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: "test@test.com"
            otp: "123456"
            client: "client"
          expect:
            - statusCode: 404
      - think: 1
