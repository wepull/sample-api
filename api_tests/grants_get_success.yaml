# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f
# 
# 

# ********RoostGPT********
config:
  plugins:
    expect: {}
  payload:
    path: "grants_get_success.csv"
    fields:
      - "accountId"
      - "tokenId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    variable:
      target: "{{ $processEnvironment.API_HOST }}"
      
scenarios:
  - name: "Retrieve accounts with granted access permissions"
    flow:
      - log: "Starting Retrieve accounts with granted access permissions"
      - get:
          url: "/grants?accountId={{ accountId }}&tokenId={{ tokenId }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 200
          - hasProperty: "grants"
          - hasProperty: "total"
          - hasHeader: "api-version"
          - headerEquals: { "api-version": "1.0.0" }
          - hasProperty: ["grants[0]", "account"]
          - hasProperty: ["grants[0]", "createdAt"]
          - hasProperty: ["grants[0]", "id"]
          - hasProperty: ["grants[0]", "token"]
          - hasProperty: ["grants[0]", "updatedAt"]
          - hasProperty: ["grants[0].account", "email"]
          - hasProperty: ["grants[0].account", "id"]
          - hasProperty: ["grants[0].account", "image"]
          - hasProperty: ["grants[0].account", "name"]
      - think: 1
