# Test generated by RoostGPT for test G683-customer-order_management using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products:search_get for http method type GET in artillery framework

# RoostTestHash=824ef8445c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  processor: "./custom-functions.js"
  plugins:
    expect: {}

  payload:
    path: "products:search_get_others.csv"
    fields:
      - "brand_name"
      - "product_name"
      - "upc"
      - "model_number"
      - "confidence_model"
      - "product_description"
    skipHeader: true
scenarios:
  - name: 'Fetch product details with fail cases'
    flow:
      # Test invalid url
      - get:
          url: "/invalidurl"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404

      # Test with missing authorization
      - get:
          url: "/products:search?brand_name={{ brand_name }}&product_name={{ product_name }}&upc={{ upc }}&model_number={{ model_number }}&confidence_model={{ confidence_model }}&product_description={{ product_description }}"
          expect:
            - statusCode: 401

      # Test with invalid parameters
      - get:
          url: "/products:search?brand_name=InvalidBrand&product_name=InvalidProduct&upc=00000000&model_number=000000&confidence_model=100&product_description=InvalidDescription"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          
      - think: 5

      # Test with correct parameters but invalid authorization
      - get:
          url: "/products:search?brand_name={{ brand_name }}&product_name={{ product_name }}&upc={{ upc }}&model_number={{ model_number }}&confidence_model={{ confidence_model }}&product_description={{ product_description }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401

