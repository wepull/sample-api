# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "accounts_post_others.csv"
    fields: ["email"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      - log: "Attempting to create account with invalid email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
      - expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                - "error.message"
                - "Invalid email format"
      - think: 1

      - log: "Attempting to create account with missing email"
      - post:
          url: "/accounts"
          headers:
            commit-hash: "abcdefg"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json: {}
      - expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                - "error.message"
                - "Email is required"
      - think: 1

      - log: "Attempting to create account with incorrect URL"
      - post:
          url: "/invalid-url"
          headers:
            commit-hash: "abcdefg"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{ email }}"
      - expect:
            - statusCode: 404
      - think: 1
