# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "./logout_post_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Logout API Test"
    flow:
      - log: "Starting Logout API Test"
      - post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: 
                api-version: "1.0.0"
      - think: 1
