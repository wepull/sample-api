# ********RoostGPT********
# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Test generated for /validateUser_post for http method type POST in artillery framework
# RoostTestHash=6833318e32



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "validateUser_post_success.csv"
    fields:
      - "sourcePin"
      - "targetPin"
    skipHeader: true

scenarios:
  - name: "Validation of successful API responses"
    flow:
      - post:
          url: "/validateUser"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sourcePin: "{{ sourcePin }}"
            targetPin: "{{ targetPin }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
