# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload: 
    path: "products_productId_get_success.csv"
    fields: 
      - "productId"
  plugins:
    expect: {}

scenarios:
  - name: "Product API Tests"
    flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Authorization"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
            # Asserting formats and data types of the properties
            - equals:
                - "{{ response.body.name }}"
                - "string"
            - equals:
                - "{{ response.body.description }}"
                - "string"
            - equals:
                - "{{ response.body.price }}"
                - "integer"
        # Adding think time
        think: 1
