# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    - expect 
  phases:
    - name: Warm up the system
      duration: 60
      arrivalRate: 1
  payload:
    path: 'products_productId_get_success.csv'
    fields:
      - 'productId'

scenarios:
  - name: Validate successful API requests
    flow:
      - auth: 
          # get the request token or key from ${$processEnvironment.AUTH_TOKEN}
          headers:                          
            {{ $seoarityScheme.name }}: "{{ $processEnvironment.AUTH_TOKEN }}"
      - log: "Request - Get detailed information of the product by using valid productId"
      - get:
          url: "/products/{{ productId }}"
          capture:
            headers:
              'content-type': 'application/json'
            json: '$.id,.name,.description,.price'
          expect:
            - hasStatusCode: 200
            - equals: 
                - "id"
                - "{{ id }}"
                - "name"
                - "{{ name }}"
                - "description"
                - "{{ description }}"
                - "price"
                - "{{ price }}"
      - think: 5
