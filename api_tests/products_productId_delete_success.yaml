# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  plugins:
    expect: {}
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Product Deletion Test"
    flow:
     # Make a DELETE request to the Products API
      - delete:
          url: "/products/{{ productId }}"
          headers:
            {{ $securitySchema.name }}: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - equals:
                Message: "Product deleted"
            - hasProperty: "productId"

      # Add a think time
      - think: 5
