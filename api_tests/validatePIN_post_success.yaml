# ********RoostGPT********
# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Test generated for /validatePIN_post for http method type POST in artillery framework
# RoostTestHash=3b396c4d7f



# ********RoostGPT********
config:
  target: "{{ process.env.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "validatePIN_post_success.csv"
    fields:
      - "sourcePin"
      - "targetPin"
      - "customerId"
      - "accountNumber"
      - "ssn"
    skipHeader: true
  environments:
    dev:
      target: "{{ process.env.API_HOST }}"
scenarios:
  - name: "Validate PINs for new user"
    flow:
      - post:
          url: "/validatePIN"
          headers:
            koreUserId: "{{ $uuid }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ process.env.AUTH_TOKEN }}"
          json:
            sourcePin: "{{ sourcePin }}"
            targetPin: "{{ targetPin }}"
            profileInfo:
              customerId: "{{ customerId }}"
              accountNumber: "{{ accountNumber }}"
              ssn: "{{ ssn }}"
          expect:
            - statusCode: 200
      - think: 1
