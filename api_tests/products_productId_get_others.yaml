# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields: ["productId"]
    skipHeader: true
    order: sequence

scenarios:
  - name: Non-successful API requests

    flow:
      # Sending a GET request without authentication
      - get:
          url: "/products/{{ productId }}"
          expect: 
          - statusCode: 401    # expecting a 401 Unauthorized status code
        think: 1

      # Sending a GET request with wrong content type
      - get:
          url: "/products/{{ productId }}"
          headers:
            content-type: "text/plain"   # using invalid content type
            {{ $processEnvironment.AUTH_TOKEN }}: ""    # using authentication
          expect: 
          - statusCode: 415    # expecting a 415 Unsupported Media Type status code
        think: 1

      # Sending a GET request to an invalid URL
      - get:
          url: "/invalidURL/{{ productId }}"  # Using an invalid URL
          headers:
            content-type: "application/json"  
            {{ $processEnvironment.AUTH_TOKEN }}: ""    
          expect: 
          - statusCode: 404    # expecting a 404 Not Found status code
        think: 1
