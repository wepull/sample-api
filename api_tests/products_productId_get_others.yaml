# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# RoostTestHash=ab8efa24ed

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}  
  payloads:
    - path: "products_productId_get_others.csv"
      fields:
        - "productId"
      skipHeader: true
  auth:
    - "Authorization"
  phases:
    - duration: 600
      arrivalRate: 5
      rampTo: 50

scenarios:
  - name: "Test for Non-Success Responses"
    flow:
      - log: "Testing for invalid product ID"
        post:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404                                            # Expected outcome when the product ID is invalid

      - think: 3

      - log: "Testing for API endpoint not found"
        get:
          url: "/invalid_endpoint/{{ productId }}"                       # API endpoint does not exist
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404                                             # Expected outcome when the API endpoint does not exist

      - think: 3

      - log: "Testing for unauthorized request"
        get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401                                             # Expected outcome when the request is unauthorized
