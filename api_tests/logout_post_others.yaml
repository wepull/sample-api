# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields: ["username", "password"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.example.com"
      processEnvironment:
        AUTH_TOKEN: "devAuthToken"

scenarios:
  - name: "Logout API Failure Cases"
    flow:
      - log: "Sending invalid GET request to /logout"
      - get:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

      - log: "Sending invalid POST request to /logout with incorrect payload"
      - post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ processEnvironment.AUTH_TOKEN }}"
          json:
            username: "wrongUsername"
            password: "wrongPassword"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending POST request to /logout without authentication"
      - post:
          url: "/logout"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending invalid URL"
      - get:
          url: "/logut"
          headers:
            ApiKeyAuth: "{{ processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
