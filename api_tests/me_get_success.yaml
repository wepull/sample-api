# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields:
      - "id"
      - "did"
      - "name"
      - "image"
      - "email"
      - "settings"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Successful API Requests"
    flow:
      - log: "Starting test scenario for successful API requests"
      - get:
          url: "/me"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
      - think: 1
