# Test generated by RoostGPT for test products-artillery using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processors/myFunctions.js"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    skipHeader: true
    fields:
      - "productId"

scenarios:
  - name: "Test suite for error scenarios"
    flow:
      # Testing with missing authorization header
      - post:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
          think: 1

      # Testing with invalid authorization token
      - post:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 403
          think: 1

      # Testing with non-existing product Id
      - delete:
          url: "/products/99999"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1

      # Testing with invalid product Id
      - delete:
          url: "/products/invalid_id"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          think: 1

      # Testing with incorrect method
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 405
          think: 1

      # Testing with incorrect URL
      - delete:
          url: "/incorrectURL/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
