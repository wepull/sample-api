# ********RoostGPT********
# Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

# Test generated for /validatePINRules_post for http method type POST in artillery framework
# RoostTestHash=44d8aeba76



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "validatePINRules_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "sourcePin"
    skipHeader: true
  plugins:
    expect: {}
      
scenarios:
  - name: "Validate PIN Rules API Test"
    flow:
      # API request to validate if pin entered by new user follows pin rules
      - post:
          url: "/validatePINRules"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sourcePin: "{{ sourcePin }}"
      - expect:
          - statusCode: 200
          - hasProperty: "description"
          - equals: 
              description: "PIN rules satisfied"
      - think: 1
