# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "credentials_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "http://dev.api.example.com"
      http:
        pool: 10
scenarios:
  - flow:
    - log: "About to delete a credential using invalid id"
    - delete:
        url: "/credentials/{{ id }}"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
    - think: 1
    - log: "About to delete a credential without authorization token"
    - delete:
        url: "/credentials/{{ id }}"
        headers:
          Authorization: ""
        expect:
          - statusCode: 401
    - think: 1
    - log: "About to delete a credential using incorrect URL"
    - delete:
        url: "/invalid/credentials/{{ id }}"
        headers:
          Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
    - think: 1
