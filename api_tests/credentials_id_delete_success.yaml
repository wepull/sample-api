# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
scenarios:
  - name: "Delete one credential from my Wallet"
    flow:
      - log: "Starting DELETE /credentials/{id} request"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
