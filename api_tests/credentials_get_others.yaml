# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending invalid GET request"
      - get:
          url: "/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Sending GET request without authorization token"
      - get:
          url: "/credentials"
          expect:
            - statusCode: 401

      - think: 1

      - log: "Sending GET request with invalid query parameters"
      - get:
          url: "/credentials?size=100&offset=-1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400

      - think: 1

      - log: "Sending valid GET request"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "credentials"
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json; charset=utf-8"]
