# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields: ["name", "email"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Invalid API usage scenario"
    flow:
      - log: "Sending a GET request to /me with invalid API key"
      - get:
          url: "/me"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Invalid API key provided"
      - think: 1

      - log: "Sending a GET request to /me with missing API key"
      - get:
          url: "/me"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                - "message"
                - "API key is missing"
      - think: 1

      - log: "Sending a GET request to an invalid URL"
      - get:
          url: "/invalidURL"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                - "message"
                - "URL not found"
      - think: 1
